/**
 * Servant implementations of the domain's central roles, notably the Cave role
 * (CaveServant) and player (PlayerServant). In addition the package contains
 * helper roles of architectural nature such as session caching
 * (PlayerSessionCache), and the dispatcher classes.
 */
package cloud.cave.server;

